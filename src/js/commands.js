/**
 * Function commands (currently none — kept for future "quick action" buttons
 * that don't open a task pane, e.g. one-click "Mark email as ERP-linked").
 */
if (typeof Office !== 'undefined') {
  Office.onReady(() => { /* noop */ });
}
